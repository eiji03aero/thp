/* -------------------- Constants -------------------- */

/* -------------------- Actions -------------------- */

/* -------------------- Initial state -------------------- */
const initialState = {
  name: 'local_user',
};

/* -------------------- Reducers -------------------- */

export const userReducer = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};
